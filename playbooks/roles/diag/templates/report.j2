=== DIAGNOSTIC REPORT — {{ diag_report.host }} ===
Generated:   {{ diag_report.timestamp }}
OS:          {{ diag_report.os }}
Severity:    {{ diag_report.severity | upper }}
Suspect     {{ diag_report.suspect_areas | join(', ') or 'none' }}

FINDINGS
{% for f in diag_report.findings %}
[{{ f.level | upper | center(4) }}] {{ f.area }}: {{ f.detail }}
{%- for line in f.log_lines %}
         | {{ line }}
{%- endfor %}
{% endfor %}

NEXT STEPS
  - Scoped check once the service is known: -e diag_services=[<service>]
{% for s in diag_report.suggested_next %}
  - {{ s }}
{% endfor %}
